# Copy this file to .env.local and adjust if your server runs on a different port/host

# Socket.io server URL (Vite proxies /socket.io in dev so this only matters for production)
VITE_SERVER_URL=http://localhost:3001
